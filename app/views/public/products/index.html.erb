<h1>商品一覧</h1>
<span>
  <% @products.each do |product| %>
  <%= image_tag product.get_product_image(200, 200) %></br>
  <%= product.name %></br>
  ￥<%= product.price.to_s(:delimited) %>
  <% end %>
</span>

