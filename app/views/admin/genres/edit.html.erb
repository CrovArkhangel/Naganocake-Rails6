<h1>ジャンル編集</h1>
<%= form_with model: [:admin, @genre] do |f| %>
  <div class="form-group">
    <%= f.label :ジャンル名 %>
    <%= f.text_field :name, autofocus: true %>
  </div>
  <%= f.submit "変更を保存" %>
<% end %>
